---
const pillars = [
  {
    title: 'Native for Agents',
    description: 'Designed for LLM consumption. High-signal context for any repository — your agent understands unfamiliar codebases without reading every file.',
    icon: 'M13 10V3L4 14h7v7l9-11h-7z',
  },
  {
    title: 'Architectural Truth',
    description: 'We surface the intent, state machines, and data flows of external repos that standard search tools miss. See the blueprint, not just the bricks.',
    icon: 'M9 3v2m6-2v2M9 19v2m6-2v2M5 9H3m2 6H3m18-6h-2m2 6h-2M7 19h10a2 2 0 002-2V7a2 2 0 00-2-2H7a2 2 0 00-2 2v10a2 2 0 002 2zM9 9h6v6H9V9z',
  },
  {
    title: 'Global Context',
    description: 'Master the Linux Kernel, React, or any critical open-source infrastructure your agent needs to work with — in a single request.',
    icon: 'M3.055 11H5a2 2 0 012 2v1a2 2 0 002 2 2 2 0 012 2v2.945M8 3.935V5.5A2.5 2.5 0 0010.5 8h.5a2 2 0 012 2 2 2 0 104 0 2 2 0 012-2h1.064M15 20.488V18a2 2 0 012-2h3.064M21 12a9 9 0 11-18 0 9 9 0 0118 0z',
  },
];
---

<section class="thesis" id="thesis">
  <div class="thesis-inner">
    <div class="section-header fade-in">
      <div class="section-label">// the_thesis</div>
      <h2>Built for the Agentic Era</h2>
      <p>Human-readable documentation is no longer the bottleneck. The world is now built by coding agents, yet they fly blind into every new repository. Gitplum is the intelligence layer that provides superior insight into any GitHub repo, instantly.</p>
    </div>
    <div class="pillars">
      {pillars.map(({ title, description, icon }) => (
        <div class="pillar fade-in">
          <div class="pillar-icon">
            <svg fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" d={icon} />
            </svg>
          </div>
          <h3>{title}</h3>
          <p>{description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .thesis {
    padding: 100px 24px;
    max-width: 1200px;
    margin: 0 auto;
  }

  .pillars {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }

  .pillar {
    padding: 40px 32px;
    background: var(--white);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    transition: all 0.3s ease;
  }

  .pillar:hover {
    border-color: var(--plum-light);
    box-shadow: var(--shadow-lg);
    transform: translateY(-4px);
  }

  .pillar-icon {
    width: 52px;
    height: 52px;
    border-radius: 14px;
    background: var(--surface-alt);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 24px;
  }

  .pillar-icon svg {
    width: 26px;
    height: 26px;
    color: var(--plum);
  }

  h3 {
    font-size: 1.188rem;
    font-weight: 700;
    margin-bottom: 12px;
    letter-spacing: -0.01em;
  }

  .pillar p {
    font-size: 0.938rem;
    color: var(--text-secondary);
    line-height: 1.7;
  }

  @media (max-width: 900px) {
    .pillars {
      grid-template-columns: 1fr;
      max-width: 520px;
      margin: 0 auto;
    }
  }
</style>
